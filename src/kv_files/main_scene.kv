<MainScene>
    coin1: coin1
    coin2: coin2
    coin3: coin3
    coin4: coin4
    coin5: coin5
    store: store
    balance: balance

    FloatLayout:

        MDLabel:
            size_hint: 1, 0.25
            pos_hint: {"center_x": .5, "top": 1}
            halign: 'center'
            id: store
            text: store.text
            theme_text_color: 'Primary'
            font_size: "25sp"

        MDIcon:
            size_hint: 1, 0.25
            pos_hint: {"center_x": .5, "top": 0.65}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'piggy-bank'
            font_size: "400sp"
            color: [0, 0, 1, 0.5]

        MDIcon:
            id: coin1
            size_hint: 1, 0.25
            pos_hint: {"center_x": .52, "top": 0.5}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'coin'
            font_size: "50sp"
            color: [0, 0, 0, 1]


        MDIcon:
            id: coin2
            size_hint: 1, 0.25
            pos_hint: {"center_x": .58, "top": 0.53}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'coin'
            font_size: "50sp"
            color: [0, 0, 0, 1]

        MDIcon:
            id: coin3
            size_hint: 1, 0.25
            pos_hint: {"center_x": .45, "top": .5}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'coin'
            font_size: "50sp"

        MDIcon:
            id: coin4
            size_hint: 1, 0.25
            pos_hint: {"center_x": .39, "top": .5}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'coin'
            font_size: "50sp"

        MDIcon:
            id: coin5
            size_hint: 1, 0.25
            pos_hint: {"center_x": .48, "top": .6}
            valign: 'bottom'
            halign: 'center'
            theme_text_color: 'Primary'
            icon: 'coin'
            font_size: "50sp"


        MDLabel:
            size_hint: 1, 0.25
            pos_hint: {"center_x": .5, "top": 0.25}
            halign: 'center'
            valign: 'bottom'
            id: balance
            text: balance.text
            theme_text_color: 'Primary'
            font_size: "25sp"
#            MDProgressBar:
#                orientation: 'vertical'
#                id: total_saving
#                max: total_saving.max
#                value: total_saving.value if total_saving.value < total_saving.max else total_saving.max
#                color: app.theme_cls.accent_color